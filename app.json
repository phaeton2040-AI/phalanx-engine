{
  "name": "phalanx-game-server",
  "description": "Phalanx Engine Game Test Server",
  "scripts": {
    "postdeploy": "echo 'Game server deployed successfully!'"
  },
  "env": {
    "NODE_ENV": {
      "description": "Environment mode",
      "value": "production"
    },
    "CORS_ORIGINS": {
      "description": "Comma-separated list of allowed CORS origins",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "stack": "heroku-24"
}
